#include "syntaxtree.h"

void syntaxTree::insertRight(const node& itemToInsert)
{

    node * current;
    current = new node;
    //current->setRight(itemToInsert);
}

void syntaxTree::insertLeft(const node& itemToInsert)
{

   // assert(current != NULL);
   // current->leftchild=itemToInsert;

}
